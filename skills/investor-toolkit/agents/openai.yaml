interface:
  display_name: "Investor Toolkit"
  short_description: "Use local research, valuation, and RSU tax tools"
  default_prompt: "Use $investor-toolkit to prepare deterministic investor data, run explicit-assumption valuations, or estimate RSU tax, then answer by reading and citing local files and command inputs."
